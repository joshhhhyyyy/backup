name: "Backup"
arch: ${NFPMARCHITECTURE}
platform: "linux"
version: ${VERSION}
section: "utils"
maintainer: "joshy <joshua@joseos.com>"
description: "A tool to backup a database via git"
homepage: "https://github.com/joshhhhyyyy/backup"
depends:
- git
contents:
- src: ${BINARYARCHITECTURE}
  dst: /usr/bin/backup
